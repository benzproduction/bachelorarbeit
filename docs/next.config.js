// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
const nextConfig = {
  output: 'export',
  basePath: '/bachelorarbeit',
  experimental: {
    appDir: true,
  },
}

module.exports = nextConfig
